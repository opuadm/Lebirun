#ifndef KEYBOARD_H
#define KEYBOARD_H

// Initialize keyboard
void init_keyboard();

// Get a single character from keyboard input
char get_char();

// Read a full line of input
char* read_line(void);

#endif
